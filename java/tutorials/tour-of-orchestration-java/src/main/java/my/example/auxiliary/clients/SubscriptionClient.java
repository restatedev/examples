package my.example.auxiliary.clients;

public class SubscriptionClient {
    public static void createSubscription(String s, String subscription, String paymentRef) {
        // Simulate creating a subscription
        System.out.println("Creating subscription for user: " + s + ", subscription: " + subscription + ", paymentRef: " + paymentRef);
    }

    public static void removeSubscription(String s, String subscription) {
    }
}
